# Database (SQLite)

File: `app.db`

This is the local database. It holds:

- companies and profiles
- imported cash-flow numbers
- users and sessions
- activity logs

Do not edit this file by hand while the app is running.  
Copy `app.db` (and `app.db-wal` / `app.db-shm` if they exist) to back up your data.
