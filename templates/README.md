# Templates

Reference PowerPoint layouts used while designing PPTX export.

These are **not** user data. User data is in `data/`.
Exported reports are downloaded by the browser; they are not stored here.
